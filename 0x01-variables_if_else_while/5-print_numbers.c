#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
	int x;

	/* print all numbers less than 10 in base 10, starting from 0 */
	for (x = 0; x < 10; x++)
	{
		printf("%d\n", x);
	}
	putchar('\n');

	return (0);
}
